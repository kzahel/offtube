var m

m = require('react')
window.React = m

m = require('react-dom')
window.ReactDOM = m

m = require('prop-types')
window.PropTypes = m

m = require('redux')
window.Redux = m

m = require('react-redux')
window.ReactRedux = m

m = require('redux-thunk')
window.ReduxThunk = m

m = require('redux-logger')
window.reduxLogger = m

m = require('@material-ui/core')
window.MaterialUI = m

m = require('bro-fs')
window.fs = m

m = require('underscore')
window._ = m

module.exports = {}
